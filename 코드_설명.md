# 온톨로지 기반 통제형 LLM 실내 환경 모니터링 시스템 — 코드 설명

---

## 1. 전체 시스템 구조 개요

논문에서 제안한 프레임워크는 세 계층으로 구성됩니다.

[센서 데이터 수집] → [온톨로지 기반 문맥 구성] → [규칙 기반 상태 판단] → [통제형 LLM 설명 생성] → [대시보드 / TTS 출력]

## 2. 센서 데이터 수집 및 온톨로지 문맥 구성 (`sensor.py`)

### 논문 대응: 3.2절 "온톨로지 기반 문맥 구조"

논문에서 온톨로지는 Room, Sensor, Measurement, Occupancy, EnvironmentState 5개 클래스로 구성됩니다. `build_ontology()` 함수가 이 구조를 Python 딕셔너리로 구현합니다.

# sensor.py - build_ontology()
return {
    'room_id': room_id,           # Room 클래스
    'type': mapping['type'],
    'hasSensor': { ... },         # Sensor 클래스 (EDC, IRC, RDC)
    'hasMeasurement': {           # Measurement 클래스
        'co2':     ...,
        'aerosol': ...,
        'temp':    ...,
        'hum':     ...,
        'stats_max': ...,         # IRC 열화상 최고온도
        'stats_avg': ...,
    },
    'hasOccupancy': ...,          # Occupancy 클래스 (RDC 레이더 기반)
    'hasState': None,             # EnvironmentState (rule_engine에서 결정)
}
```

핵심 포인트: 원시 센서 수치가 의미 있는 문맥 정보(hasMeasurement, hasOccupancy 등)로 변환됩니다. 논문 표 1의 "원본 데이터 → 문맥 변환"이 이 함수에 해당합니다.

### 센서 종류별 역할

| 센서 | 변수 | 논문 역할 |
|------|------|-----------|
| EDC (복합환경센서) | co2, aerosol, temp, hum | 환경 측정값 (Measurement) |
| RDC (레이더) | target_count | 재실 주 판단 (Occupancy) |
| IRC (열화상) | stats_max, stats_avg | 과열 탐지 + 재실 보조 판단 |

IRC의 재실 보조 감지는 RDC가 없는 공간(hasOccupancy=None)에서만 작동합니다 — 이는 `rule_engine.py`에 구현되어 있습니다.

---

## 3. 규칙 기반 상태 판단 (`rule_engine.py`)

### 논문 대응: 4.3절 "규칙 기반 상태 판단"

논문 표 3의 상태 분류 기준을 그대로 코드로 구현한 부분입니다.

```python
# rule_engine.py - 상태 판단 흐름
def rule_engine(o):
    # 1단계: CO2, PM2.5 기준 초과 여부 확인 → status 결정
    if co2 > th['co2']['normal']:
        status = 'danger'
    elif co2 > th['co2']['comfortable']:
        status = 'normal'

    # 2단계: 복합 이상 2개 이상이면 위험도 상향
    if anomaly >= 2 and status == 'normal':
        status = 'danger'

    # 3단계: IRC 열화상 50°C 이상 → 강제 danger
    if stats_max >= 50:
        status = 'danger'

    # 4단계: 재실 여부 + 상태 조합 → 최종 상태 결정 (논문 표 3)
    if occupied is True and status == 'danger':
        → danger (즉각 조치 필요)
    elif occupied is False and status == 'danger':
        status = 'abnormal'  # 무재실 위험 → 비정상
```

### 임계값 설정 (`config.py`)

논문 4.3절에서 언급한 "학교보건법 시행규칙, 실내공기질관리법" 기준이 `THRESHOLDS` 딕셔너리에 반영되어 있습니다.

```python
# config.py
THRESHOLDS = {
    'classroom': {
        'co2':     {'comfortable': 700, 'normal': 1000},  # ppm
        'aerosol': {'comfortable': 15,  'normal': 35},    # μg/m³
        'temp':    {'min': 18, 'max': 28},                # °C
        'hum':     {'min': 30, 'max': 80},                # %
    },
    'hall': {                                             # 홀은 온도·습도 기준 없음
        'aerosol': {'comfortable': 25, 'normal': 50},    # 기준 완화
    },
}
```

공간 유형(classroom vs hall)에 따라 PM2.5 기준이 다른 것이 논문 표 2와 일치합니다.

---

## 4. 통제형 LLM 기반 환경 해석 (`llm.py`)

### 논문 대응: 5절 "통제형 LLM 기반 환경 해석"

논문의 핵심 기여인 **Prompt Grounding** 구조가 이 파일에 구현됩니다.

```python
# llm.py - llm_explain()
prompt = f"""당신은 실내 환경 모니터링 시스템의 음성 안내를 생성합니다.

아래 센서 데이터와 판단 결과만을 기반으로 안내하세요.
절대 데이터에 없는 원인이나 상황을 추측하지 마세요.    ← 환각 방지 핵심 지시

[공간 정보] 공간: {o['room_id']} / 재실 인원: {count}명  ← Ontology-guided Context
[센서 데이터] CO2: {co2}ppm / PM2.5: {aerosol}μg/m³ ...  ← Measurement 삽입
[판단 결과] 상태: {status_kr} / 근거: {reason}            ← Rule-based 결과 주입

출력 규칙:
- TTS로 읽기 좋게 2문장 이내                              ← 출력 길이 제한
- 위험 상태에서만 핵심 위험 항목 1개 설명                  ← 과잉 행동 지침 방지
- ppm, μg/m³ 같은 단위는 읽지 말 것                       ← TTS 최적화
"""
```

### 논문 표 5와의 대응 (일반 LLM vs 온톨로지 LLM vs 통제형 LLM)

| 구분 | 이 코드 | 논문 설명 |
|------|---------|-----------|
| 입력 구조 | 규칙 판단 결과 + 문맥 정보 포함 | Ontology-guided + Rule-based |
| 상태 판단 | rule_engine이 결정, LLM은 설명만 | LLM 의존 없음 |
| 행동 지침 | 1가지만, 필요한 경우만 | 출력 제한 |
| 환각 방지 | 프롬프트에 명시적 금지 | Prompt Grounding |

---

## 5. 실험 평가 (`tests.py`)

### 논문 대응: 6.2절 "실험 결과 — SCR, HR 비교"

논문의 실험에서 세 가지 LLM 구조를 비교합니다. `tests.py`가 이 실험을 자동화합니다.

```python
# tests.py - 3가지 구조 비교

# 1) LLM Only: 센서 수치만 던져주는 비통제 프롬프트
def prompt_uncontrolled(o):
    return f"CO2: {co2}ppm, PM2.5: {aerosol}μg/m³ ... 상태를 설명해줘"

# 2) LLM + Ontology: 구조화된 문맥 포함하지만 규칙 판단 없음
def prompt_ontology(o):
    return f"공간유형: {type}, CO2: {co2}, 재실: {count}명 ... 설명해줘"

# 3) Controlled LLM (논문 제안): rule_engine 결과 + 통제 프롬프트
ctrl = llm_explain(judged)   # llm.py의 함수 사용
```

**평가 지표**:
- SCR (State Consistency Rate, 상태 일치율)**: LLM 출력에 올바른 상태 키워드가 있고 반대 키워드가 없는 비율
- HR (Hallucination Rate, 환각 발생률)**: 극단적 과장 표현 또는 상태 불일치 과잉 표현 감지 비율

실험 결과 통제형 LLM의 SCR은 96.7%, HR은 0.0%로, 일반 LLM(SCR 40.0%, HR 40.0%)과 온톨로지 LLM(SCR 13.3%, HR 66.7%)보다 크게 개선되었습니다.

## 6. API 서버 및 WebSocket (`main.py`)

### 논문 대응: 3.3절 "WebSocket 기반 대시보드 및 TTS"

python
# main.py - 데이터 흐름

async def run_all():
    # 1. DB에서 최신 공간 매핑 로드
    ROOM_MAPPING = await asyncio.to_thread(load_room_mapping)

    # 2. 모든 공간 병렬 처리 (온톨로지 구성 + 규칙 판단)
    results = await asyncio.gather(*[judge_room(room_id, mapping) for ...])

    # 3. (선택) LLM 설명 추가
    if include_llm:
        results = await asyncio.gather(*[add_llm(r) for r in results])

    return results
```

- `GET /api/rooms`: 빠른 센서 데이터 (LLM 제외)
- `GET /api/rooms?include_llm=true`: LLM 설명 포함
- `WebSocket /ws`: 5초 주기 실시간 푸시

### 에어컨 자동 제어 (`ac.py`)

```python
# ac.py - apply_ac()
# 위험(danger) + 재실 → 에어컨 ON, 온도에 따라 22°C or 24°C 설정
if state == 'danger' and occupied is True:
    await ac_control(device_id, 'POWER_ON', target)
else:
    await ac_control(device_id, 'POWER_OFF')
```

LG ThinQ API를 통해 에어컨을 제어하며, 규칙 기반 판단 결과를 실제 기기 제어로 연결합니다.

---

## 7. 프론트엔드 (`src/`)

### 논문 대응: 3.4절 "실시간 대시보드 및 TTS 음성 안내"

| 파일 | 역할 |
|------|------|
| `App.js` | WebSocket 연결, 2단계 로딩 (센서 먼저 → LLM 나중) |
| `FloorLayout.js` | 2층/3층 평면도 미니맵 + 공간 카드 그리드 |
| `RoomCard.js` | 공간별 CO₂, PM2.5, 온도, 습도 + 상태 배지 |
| `DetailPanel.js` | 상세 패널 + **브라우저 TTS** 음성 안내 버튼 |
| `TrendChart.js` | CO₂ 추세 차트 + 기준치 초과 예측 시각화 |

**TTS 구현** (`DetailPanel.js`):
```javascript
const speakLlm = (text) => {
    const utterance = new SpeechSynthesisUtterance(text);
    utterance.lang = 'ko-KR';
    window.speechSynthesis.speak(utterance);
};
```
LLM이 생성한 자연어 설명을 브라우저 Web Speech API로 음성으로 출력합니다.

---

## 8. 전체 데이터 흐름 요약

```
DB (MariaDB)
 └─ sensor.py: build_ontology()
     └─ EDC: CO2, PM2.5, 온도, 습도 평균
     └─ RDC: 재실 인원 합계
     └─ IRC: 열화상 최대/평균
         ↓
rule_engine.py: rule_engine()
     └─ 임계값 비교 (config.py THRESHOLDS)
     └─ 복합 이상, 재실 조합 → comfortable/normal/danger/abnormal
         ↓
llm.py: llm_explain()  [include_llm=True인 경우]
     └─ 통제형 프롬프트 → Ollama (Gemma4-E4B)
     └─ 자연어 2문장 이내 설명 생성
         ↓
main.py: FastAPI REST + WebSocket
     └─ 프론트엔드(React) → 대시보드 표시 + TTS 음성 출력
     └─ ac.py → LG ThinQ 에어컨 자동 제어
```

---

*논문 GitHub: https://github.com/parkjuho12/kbu_serdic*
